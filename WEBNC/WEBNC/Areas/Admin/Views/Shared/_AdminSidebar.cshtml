@{
    string currentArea = ViewContext.RouteData.Values["area"]?.ToString() ?? "";
    string currentController = ViewContext.RouteData.Values["controller"]?.ToString() ?? "";
    string currentAction = ViewContext.RouteData.Values["action"]?.ToString() ?? "";
}

<div class="sidebar p-4">
    <div class="fw-bold fs-5 mb-3">ADMIN</div>

    <div class="small text-white-50 mb-3">
        @currentArea / @currentController / @currentAction
    </div>

    <a class="@(currentController == "Home" ? "active" : "")"
       asp-area="Admin" asp-controller="Home" asp-action="Index">
        🏠 Dashboard
    </a>

    <a class="@(currentController == "SanPham" ? "active" : "")"
       asp-area="Admin" asp-controller="SanPham" asp-action="Index">
        📦 Sản phẩm
    </a>

    @* Bạn làm tiếp CRUD các mục này thì chỉ cần mở comment *@
    @*
    <a class="@(currentController == "LoaiSanPham" ? "active" : "")"
       asp-area="Admin" asp-controller="LoaiSanPham" asp-action="Index">
        🧩 Loại sản phẩm
    </a>

    <a class="@(currentController == "CongTy" ? "active" : "")"
       asp-area="Admin" asp-controller="CongTy" asp-action="Index">
        🏢 Công ty
    </a>

    <a class="@(currentController == "DonDatHang" ? "active" : "")"
       asp-area="Admin" asp-controller="DonDatHang" asp-action="Index">
        🧾 Đơn đặt hàng
    </a>
    *@
</div>
